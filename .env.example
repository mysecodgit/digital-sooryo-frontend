## Copy to `.env` for local development.
## Vite only exposes env vars prefixed with `VITE_`.

# Base public URL for claim links (used when building QR claim URLs)
VITE_PUBLIC_APP_URL=
VITE_APP_DEFAULTAUTH=